import { FC, memo } from "react"
import Styled from "./styled"

const Banner: FC = () => {
    return (
        <Styled.Banner>
            sad
        </Styled.Banner>
    );
};

export default memo(Banner);