import React, { memo } from "react";
import * as Styled from "@/components/organisms/Header/styled";

const Header = () => {
  return <Styled.Header>Header</Styled.Header>;
};

export default memo(Header);
