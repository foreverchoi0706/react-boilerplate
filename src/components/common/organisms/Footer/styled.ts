import styled, {css} from "styled-components";

export const Footer = styled.footer(
  () => css`
    background-color: lightgray;
    max-width: 1200px;
    margin: 0 auto;
    padding: 15px;
  `
);
